<%= form_for(@author) do |f| %>
    <% if @author.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@author.errors.count, "error") %> prohibited this product from being saved:</h2>
          <ul>
            <% @author.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </div>
    <div class="actions">
      <button id="whatever" class="btn btn-primary" name="submit" type="submit">
        <span>Save</span>
        <i class="icon-ok" style="font-size:20px; vertical-align: middle;"></i>
      </button>
    </div>
<% end %>

